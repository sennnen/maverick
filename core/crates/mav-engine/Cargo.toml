[package]
name = "mav-engine"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
mav-model = { workspace = true }
mav-connector-abi = { workspace = true }
mav-connector-runtime = { workspace = true }
mav-frame = { workspace = true }
mav-codec = { workspace = true }
mav-sqi = { workspace = true }
mav-timeline = { workspace = true }
mav-feature = { workspace = true }
mav-analytic = { workspace = true }
mav-store = { workspace = true }
mav-obs = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
# Tests only: the runtime tests exercise the register_codec seam with the real WHOOP codec, the
# same way the FFI does. The runtime dependency graph stays device-free (check_deps ignores
# dev-dependencies deliberately).
mav-connector-whoop = { workspace = true }

[lints]
workspace = true
